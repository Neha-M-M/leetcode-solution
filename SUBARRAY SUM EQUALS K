class Solution:
    def subarraySum(self, nums, k):
        count = 0
        total = 0
        d = {0: 1}

        for num in nums:
            total += num
            if total - k in d:
                count += d[total - k]
            d[total] = d.get(total, 0) + 1

        return count
